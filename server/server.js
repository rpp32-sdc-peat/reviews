const app = require('./index.js');
const port = 3000;

// if (process.env.NODE_ENV !== 'test') {
//   app.listen(port, () => console.log('Listening on:', port));
// }

// app.listen(port, () => console.log('Listening on:', port));